import { View, Text } from "react-native";
import styles from "../Styles";
import React from "react";

const Header = () => {
  return (
    <View style={styles.center}>
      <Text style={styles.heading}>குணா வெல்டிங் ஒர்க்ஸ் </Text>
    </View>
  );
};

export default Header;
